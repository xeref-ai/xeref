
'use client';

import { TodayFocusPanel } from '@/components/today-focus-panel';

export default function TodayFocusPage() {
  return <TodayFocusPanel onClose={() => { }} onAddToChat={() => { }} />;
}
