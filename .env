OPEN_ROUTER_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
# --- Client-side Firebase Config ---
# To find these values, go to your Firebase project overview,
# click the "</>" icon to add a web app, and copy the config.
# These variables should be prefixed with NEXT_PUBLIC_ and go in your .env.local file.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_DATABASE_URL=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
# --- Server-side Firebase Admin SDK Config ---
# This should go in your .env.local file. DO NOT commit it.
# 1. In your Firebase project, go to Project Settings > Service Accounts.
# 2. Click "Generate new private key" and a JSON file will download.
# 3. Open the downloaded file and copy its ENTIRE content.
# 4. Paste the full JSON content as a single line for the variable below.
# Example: FIREBASE_SERVICE_ACCOUNT_KEY='{"type": "service_account", ...}'

